{
  "name": "smartprice-egypt-proxy",
  "version": "1.0.0",
  "description": "SerpApi proxy server for SmartPrice Egypt — no CORS, no exposed keys",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
